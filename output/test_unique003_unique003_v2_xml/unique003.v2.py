from output.models.saxon_data.complex.unique003_xsd.unique003 import Root


obj = Root(
    sub=[
        Root.Sub(
            idelt=17.1
        ),
        Root.Sub(
            idelt=float("nan")
        ),
    ]
)
