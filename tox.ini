[tox]
envlist = py37
skip_missing_interpreters = true
isolated_build = True
skipsdist=True

[testenv]
passenv = TOXENV CI TRAVIS TRAVIS_*
deps =
    pytest
    pytest-xdist
    xmlschema
    git+https://github.com/tefra/xsdata

commands =
    pytest -n auto {posargs}
